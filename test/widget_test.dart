import 'package:flutter_test/flutter_test.dart';

void main() {
  group('App', () {
    test('placeholder test - will add widget tests later', () {
      // Placeholder test until we set up proper widget testing with DI mocks
      expect(true, true);
    });
  });
}
